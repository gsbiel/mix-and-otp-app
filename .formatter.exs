# Used by "mix format"
# Esse comando vai formatar os arquivos com as extensões definidas aqui, removendo espaçamentos e quebras de linhas extras do código, tudo seguindo um padrão configurável. 
[
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"]
]
